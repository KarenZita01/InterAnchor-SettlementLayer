[package]
name = "settlement-engine-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
settlement-engine = { path = "../settlement-engine" }

[dev-dependencies]
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
